<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.powertac</groupId>
    <artifactId>server-master</artifactId>
    <version>1.3.2</version>
    <relativePath />
  </parent>

  <artifactId>evcustomer</artifactId>
  <version>1.3.3-SNAPSHOT</version>
  <name>Power TAC ev customer</name>
  <description>Bottom-up model of a electric vehicle customer</description>

  <repositories>
    <repository>
      <id>sonatype</id>
      <url>https://oss.sonatype.org/content/repositories/snapshots/</url>
    </repository>
  </repositories>
  
  <properties>
    <aspectj-plugin.version>1.8</aspectj-plugin.version>
    <aspectj-plugin.compliance>1.8</aspectj-plugin.compliance>
    <commons-math.version>3.5</commons-math.version>
    <commons-io.version>2.4</commons-io.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.powertac</groupId>
      <artifactId>server-interface</artifactId>
      <version>${powertac.version}</version>
    </dependency>
    <dependency>
      <groupId>org.apache.commons</groupId>
      <artifactId>commons-math3</artifactId>
      <version>${commons-math.version}</version>
      <scope>compile</scope>
    </dependency>
    <dependency>
      <groupId>commons-io</groupId>
      <artifactId>commons-io</artifactId>
      <version>${commons-io.version}</version>
      <scope>compile</scope>
    </dependency>
  </dependencies>

  <!-- needs AspectJ build -->
  <build>
    <plugins>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>aspectj-maven-plugin</artifactId>
        <version>${aspectj-plugin.version}</version>
        <executions>
          <execution>
            <goals>
              <goal>compile</goal>
              <goal>test-compile</goal>
            </goals>
            <configuration>
              <verbose>false</verbose>
              <complianceLevel>${aspectj-plugin.compliance}</complianceLevel>
              <weaveDependencies>
                <weaveDependency>
                  <groupId>org.powertac</groupId>
                  <artifactId>common</artifactId>
                </weaveDependency>
              </weaveDependencies>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
  
  <scm>
    <connection>scm:git:git//github.com/powertac/evcustomer.git</connection>
    <developerConnection>scm:git:git@github.com:powertac/evcustomer.git</developerConnection>
    <url>https://github.com/powertac/evcustomer</url>
  </scm>
</project>
