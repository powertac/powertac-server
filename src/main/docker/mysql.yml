version: '2'
services:
    visualizer2-mysql:
        container_name: visualizer2-mysql
        image: mysql:5.7.14
        # volumes:
        #     - ~/volumes/jhipster/visualizer2/mysql/:/var/lib/mysql/
        environment:
            - MYSQL_USER=root
            - MYSQL_ALLOW_EMPTY_PASSWORD=yes
            - MYSQL_DATABASE=visualizer2
        ports:
            - 3306:3306
        command: mysqld --lower_case_table_names=1 --skip-ssl
